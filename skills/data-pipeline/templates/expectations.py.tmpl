# Generated by data-pipeline (toolkit) -- data quality expectations for $target_table, derived
# from its data-contract tests[] (see references/toolkit-conventions.md #5: tests a human/discovery
# already established become enforced expectations here, not re-derived).
#
# EXPECTATIONS values are read by declarative_pipeline.py's apply_changes / a wrapping
# @dlt.expect_all_or_drop(EXPECTATIONS) decorator -- wire them in per
# references/declarative-pipelines.md.

EXPECTATIONS = {
$expectation_lines
}
