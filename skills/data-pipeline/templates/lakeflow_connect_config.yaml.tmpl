# Generated by data-pipeline (toolkit) -- Lakeflow Connect ingestion config for $target_table.
#
# Lakeflow Connect ingestion is CONFIGURED (via the Databricks UI, CLI, or Terraform provider),
# not hand-coded -- this file is a stub naming the pieces a human fills in, not an executable
# artifact. See references/lakeflow-connect.md. Generating this file does not create, configure,
# or start any ingestion connector -- see references/toolkit-conventions.md #1 and #7.

connector_type: "<fill in: e.g. salesforce, sql_server, workday, servicenow>"
source_object: "$source_schema.$source_table"
destination:
  catalog: "$target_catalog"
  schema: "$target_schema"
  table: "$target_table"
merge_keys: $merge_keys_pylist
sync_schedule: "<fill in, e.g. every 1 hour -- Lakeflow Connect manages CDC/incremental state internally>"
